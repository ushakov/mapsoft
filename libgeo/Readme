geo_data.h  -- геоданные
geo_convs.h -- преобрадования геоданных
geo_types.h -- типы данных, используемые в geo_data.h
geo_refs.h  -- привязки конкретных космоснимков

TODO

* Оторвать зависимость от loaders (перенести определение размеров
  карты в layer_geomap, здесь же тупо использовать ту границу, которая
  есть)

* Оторвать зависимость от utils (видимо, надо куда-то унести из utils
  описания типов (image, color, time, options). image в 2d, остальное -
  сюда?

* Оторвать зависимость от layers? (унести geo_refs в layers/?)