Несколько утилит для работы с треками

mapsoft_mark

   На вход получает треки. Генерирует точки, размечающие треки через
   километр.

mapsoft_measure

   Измерение длины треков (и скорости, если треки содержат информацию о
   времени)

mapsoft_trackprofile

   Построение высотного профиля по треку. Данные по высоте берутся из
   трека. Высотный профиль выдается в виде CSV-файла.

mapsoft_srtmprofile

   Построение высотного профиля по треку. Данные по высоте берутся по
   SRTM данным. Высотный профиль выдается в виде CSV-файла.
