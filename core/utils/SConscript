Import ('env_cairo')
e=env_cairo.Clone()

e.Library('utils', \
  Split("""
    log.cpp
    iconv_utils.cpp spirit_utils.cpp
    pnm_shifter.cpp
    cairo_wrapper.cpp
  """))

SConscript ("tests/SConscript")
