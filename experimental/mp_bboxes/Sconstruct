env = Environment ()

mapsoft_libs=Split("mp utils 2d")

env.Append (CCFLAGS=['-O2'])
env.Append (CPPPATH = ["../../core"])
env.Append (LIBPATH = map(lambda(s): "../../core/"+s, mapsoft_libs) )
env.Append (LIBS = [mapsoft_libs] )

env.Program(["mp_bboxes.cpp"])
env.Program('mp_bboxes2.cpp')


