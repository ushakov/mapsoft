Import ('env')

env.MLib("viewer",
         srcs = Split("""
                  generic_dialog.cpp
                  action_manager.cpp
                  workplane.cpp
                  """),
         deps = Split("""
                  options
                  2d
                  gred
                  """))

env.MProg('mapsoft_mapview',
          srcs = ['mapsoft_mapview.cpp'],
          deps = Split("""
                   viewer
                   geo_io
                   utils
                   gred
                   """))

