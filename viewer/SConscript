Import ('env')
le = env.Clone()
le.ParseConfig('pkg-config --cflags --libs gtkmm-2.4,gthread-2.0')

le.Library("viewer", \
    Split ("""
	generic_dialog.cpp
	action_manager.cpp
	viewer.cpp
    """))

