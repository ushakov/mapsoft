Программы для массового скачивания карт с сайта poehali.org
На данный момент не работают, poehali.org опять изменился.

На момент, когда скриптики работали, скачивание осуществлялось таким
образом:

   1) При помощи init-cookie нужно было один раз инициализировать Cookie
   для скачивания карт. Инициализация Cookie производилась автоматически
   или введением кода.

   2) При помощи list-maps можно было получить список карт, содержащих
   точку с заданными координатами. При помощи list-region можно было
   получить список карт покрывающих прямоугольный регион.
   Формат параметров можно посмотреть в подсказке, запустив list-maps и
   list-region без параметров.

   3) При помощи get-map можно было скачать конкретную карту.
   При помощи простого скрипта на shell-е типа
      for map in `list-maps 61.242495831955 31.742935180578`; do
         get-map $map
      done
   можно скачать все найденные на втором шаге карты.
   get-map понимает разные форматы названий карт, подробности можно
   получить, запустив get-map без параметров.

В директории lists лежат списки карт на poehali.org по состоянию
примерно на 2007-2008 годы.
